# JSON files are large but not needed for Streamlit dashboard
# Keep local, don't commit to git (add to .gitignore)
Data/edgar/form499_registry.xml filter=lfs diff=lfs merge=lfs -text
Data/edgar/cik-lookup-data.txt filter=lfs diff=lfs merge=lfs -text
# Essay 3 Query 2 chain inputs (2026-09-11): committed as LFS pointers but lacked the attribute since 5f5c950, so a
# clean clone checked out raw pointer text. Objects verified on GitHub (LFS batch API). The other LFS pointer files
# without an attribute are NOT re-enabled here (LFS quota history; see 5f5c950).
Data/wrds/crsp_daily_returns.csv filter=lfs diff=lfs merge=lfs -text
Data/wrds/market_indices.csv filter=lfs diff=lfs merge=lfs -text
Data/wrds/compustat_annual.csv filter=lfs diff=lfs merge=lfs -text
Data/wrds/compustat_fundamentals.csv filter=lfs diff=lfs merge=lfs -text
Data/wrds/ticker_permno_mapping.csv filter=lfs diff=lfs merge=lfs -text
Data/processed/DATA_DICTIONARY_ENRICHED.csv filter=lfs diff=lfs merge=lfs -text
# Frozen classifier (6f7be7a): keep LF on every checkout so its logged sha256 (42a64e12…) reproduces on Windows
scripts/195_essay3_q2_classifier_v2.py text eol=lf

# ==============================================================================
# REBUILD V4 (branch rebuild-v4) — added at Stage 0, before any file exists under
# Data/wrds_v4/. Point-in-time CRSP/Compustat pulls are large binaries-by-size;
# they must be LFS-tracked from the first add so no raw blob ever enters history.
# Appended only; no existing rule above is modified.
# ==============================================================================
Data/wrds_v4/** filter=lfs diff=lfs merge=lfs -text
# Frozen v3 input under the REBUILD V4 freeze: pin LF so the working-tree bytes match the
# manifest sha (4a46a82a…) on every platform. Without this, core.autocrlf=true renders it
# CRLF on checkout, flipping the disk sha to 0d8e556c… and forcing scripts/210 to pass it
# through the eol-artifact branch. Same pattern as scripts/195 above.
scripts/204_essay3_q2_se_diagnostics.py text eol=lf
